# Claude Code local settings (may contain secrets, never commit)
.claude/settings.local.json

# Env / secrets
.env
.env.local
.env.*.local
*.key
*.pem
wallets.json

# Scripts folder (contains private keys and wallet data)
scripts/
